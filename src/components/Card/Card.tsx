import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const Card = (props: Props) => {
    return (
        <View>
            <Text>Card</Text>
        </View>
    );
};

export default Card;
